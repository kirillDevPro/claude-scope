name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'

      # See ci.yml: the postinstall skill installer is a bash script, and a
      # release must not depend on the runner's shell.
      - name: Install dependencies
        run: npm ci --ignore-scripts

      # The pinned Biome from devDependencies - see ci.yml.
      - name: Run Biome checks
        run: npx biome ci .

      - name: Build project
        run: npm run build

      # Runs the whole suite under c8, so a separate `npm test` step would only
      # run all 1052 tests a second time. Thresholds come from .c8rc.json, and
      # a release that misses them is not one to publish.
      - name: Run tests with coverage
        run: npm run test:coverage

      - name: Publish to npm
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
