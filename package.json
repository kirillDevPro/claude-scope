{
  "name": "claude-scope",
  "version": "1.1.0",
  "description": "Claude Code plugin for session status and analytics",
  "license": "MIT",
  "type": "module",
  "main": "dist/claude-scope.cjs",
  "bin": {
    "claude-scope": "dist/claude-scope.cjs"
  },
  "files": [
    "dist/claude-scope.cjs",
    ".claude/skills/",
    "scripts/install-skill.sh",
    "README.md",
    "LICENSE.md"
  ],
  "scripts": {
    "postinstall": "bash scripts/install-skill.sh 2>/dev/null || true",
    "prepack": "npm run build",
    "build": "npm run build:tsc && npm run build:bundle",
    "build:tsc": "tsc",
    "build:bundle": "esbuild src/index.ts --bundle --platform=node --target=node18 --outfile=dist/claude-scope.cjs && chmod +x dist/claude-scope.cjs",
    "prebuild:bundle": "npm run build:tsc",
    "test": "node scripts/run-tests.mjs",
    "test:unit": "node scripts/run-tests.mjs unit",
    "test:integration": "node scripts/run-tests.mjs integration",
    "test:e2e": "node scripts/run-tests.mjs e2e",
    "test:list": "node scripts/run-tests.mjs --list",
    "test:coverage": "c8 --check-coverage --lines=80 --functions=75 --statements=80 --branches=65 --reporter=text --reporter=html --exclude='tests/**' --exclude='scripts/**' --exclude='**/*.test.ts' node scripts/run-tests.mjs",
    "test:snapshot": "node scripts/run-tests.mjs --update-snapshots",
    "test:snapshot:verify": "npm test",
    "dev": "tsx src/index.ts",
    "prepare": "husky",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13",
    "@types/node": "^22.10.2",
    "c8": "^10.1.3",
    "chai": "^6.2.2",
    "esbuild": "^0.27.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "rimraf": "^6.1.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "lint-staged": {
    "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}": [
      "biome check --write --no-errors-on-unmatched --files-ignore-unknown=true"
    ]
  },
  "dependencies": {
    "@inquirer/prompts": "^7.10.1",
    "systeminformation": "^5.30.3"
  }
}
